name: Test

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      
      - name: Run a one-line script
        run: echo "Hello from TestProject CI!"
      
      - name: Check directory contents
        run: ls -la
      
      - name: Always pass
        run: echo "Tests complete" && exit 0
